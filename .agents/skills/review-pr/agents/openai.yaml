interface:
  display_name: "Review PR"
  short_description: "Review pull requests without modifying code"
  default_prompt: "Use $review-pr to produce severity-ranked findings and a structured handoff for $prepare-pr."
